*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
build/
*.hprof
*.keystore
!debug.keystore
# A release signing key must never land in the repository: anyone holding it can publish an
# update that Android will install over yours.
*.jks
/dist/

# Training artefacts: models and features are large and regenerable.
training/.venv/
training/data/
training/models/
__pycache__/
*.pyc

# Kotlin build tooling scratch, written next to the sources it compiles.
.kotlin/
